[package]
name = "nimrs-firmware"
version = "0.1.0"
edition = "2021"
resolver = "2"

[dependencies]
esp-idf-sys = { path = "vendor/esp-idf-sys" }

[package.metadata.esp-idf-sys]
esp_idf_tools_install_dir = "fromenv"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = "symbols"
